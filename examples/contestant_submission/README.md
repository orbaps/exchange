# Example Team Submission

This is a mock contestant submission demonstrating the required folder structure:
- `metadata.json`
- `engine.py`

This engine does not implement matching logic, but it does expose the correct methods to be successfully wrapped by `ContestantSubmissionAdapter`.
